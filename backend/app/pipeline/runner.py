"""Orquestração do pipeline parse → extract → analyze.

Stub nesta issue (#8): os estágios reais entram nas issues #9 e #10.
"""


async def run_pipeline(contract_id: str) -> None:
    # Intencionalmente vazio: o contrato permanece em "processing" até os
    # estágios reais existirem. Os testes de upload cobrem apenas o agendamento.
    return None
